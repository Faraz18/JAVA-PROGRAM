import java.util.Scanner;

public class Main
{
	public static void main(String[] args) {
	    Scanner sc= new Scanner(System.in);
	    int a = 0, b = 1, i, nt;
		System.out.println("Enter the no of outcomes:");
		int n= sc.nextInt();
		for (i = 1; i <= n; i++)
		{
		    System.out.println(a);
		    nt = a + b;
		    a = b;
		    b = nt;
    }
	}
}
